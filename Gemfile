source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "jekyll-remote-theme"
gem "csv"       # already added for the csv load error
gem "base64"    # new line to fix the base64 error
gem "logger"    # optionally, to preempt the “logger was loaded” warnings
